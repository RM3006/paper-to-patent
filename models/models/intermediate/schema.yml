version: 2

models:
  - name: int_org_crosswalk
    description: >
      Org crosswalk sourced from R2 — re-exposes Part 3 output inside the dbt graph.
    columns:
      - name: org_id
        tests: [not_null]
      - name: source
        tests: [not_null]
      - name: source_id
        tests: [not_null]
      - name: match_method
        tests: [not_null]
      - name: confidence
        tests: [not_null]
